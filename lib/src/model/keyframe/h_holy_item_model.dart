//A model that holds the selected item in [HolyHorizantalPicker] .
import 'package:flutter/material.dart';

class HSelectedItem {
  String value;
  int index;
  HSelectedItem({
    @required this.value,
    @required this.index,
  });
}
